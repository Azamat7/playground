# Playground on Flask + React

## Installation

inside playground:

```bash
pip install -r requirements.txt
```

inside my-app:

```bash
yarn 
```

## Running

inside playground:

```bash
python app.py
```

inside my-app:

```bash
yarn run start
```